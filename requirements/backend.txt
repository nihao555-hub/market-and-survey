# 第一环节·选品/市场调研 — 后端服务栈（FastAPI 控制平面 + 队列 + 存储）
# 安装：pip install -r requirements/backend.txt（会自动带上 core.txt）
-r core.txt

# Web 框架 + SSE
fastapi==0.137.1
uvicorn[standard]==0.49.0
sse-starlette==3.4.4

# GraphQL（Query/Mutation/Subscription over graphql-sse）
strawberry-graphql==0.317.1

# 队列 + pub/sub（任务持久化、cancel 信号、SSE 重放）
redis==8.0.0
dramatiq[redis]==2.2.0

# 持久化：默认 SQLite；设了 DATABASE_URL 时走 Postgres/MySQL/OceanBase。
SQLAlchemy==2.0.51
psycopg[binary]==3.2.3
PyMySQL==1.1.1
cryptography==44.0.3

# 每日定时刷新（北京 0 点 = UTC 16:00 的 cron 调度）
APScheduler==3.11.2

# ── 实测必需的轻量爬虫引擎（默认安装）──
# 9 级降级链的 L1 快速通道：TLS/JA3 指纹伪装（1688/MIC/DHgate 抓取链都依赖它，
# 不装则首部署时 L1 层全缺）。重型浏览器引擎仍在 requirements/scrapers.txt 可选安装。
curl-cffi>=0.15
# 失败重试工具（抓取编排 + IP 接口重试）
tenacity>=8.2
