# ① 选品 — 数据真实性彻底修复进度

> 用户提了 5 个严肃问题，必须全部彻底修。本文档诚实记录修复进度。

---

## ✅ 已彻底修复

### 1. 删掉所有"按品类经验估算 25-35%"
- 文件：`modules/sourcing_1688.py`
- 现状：失败时**只返回 error + alternative_data_sources**，明确告诉 Agent "1688 被验证码挡了，请走替代数据源"
- LLM 不再被诱导用 fallback 数字

### 2. 删掉 BSR→月销量公式（部分）
- 文件：`modules/bestsellers.py`
- 现状：返回 `estimated_monthly_sales_low / high` 区间 + 明确 `real_data: false` + 警告"这是区间估算，非真实销量"
- LLM 知道这是估算

### 3. 取消硬编码类目 dict + LLM 自由扩展
- 文件：`modules/bestsellers.py`
- 现状：
  - `discover_bsr_url(category_keyword)` 让 LLM 给关键词
  - 内置高频跨境品类的已知 BSR URL（10 个品类）+ 兜底搜索 URL
  - Amazon 搜索页 SPA 化后，"完全动态发现"暂无可行方案
- LLM 收到的是"候选 URL 列表 + 用法说明"

### 4. 真实成本数据源（关税+佣金+FBA+汇率）
- 文件：`modules/real_cost_data.py`
- 现状：
  - **关税**：USITC HTS 内置查表（耳机 4.5% / 手表 5.8% 等真值）
  - **Amazon Referral Fee**：2026 真实费率表（耳机 8% 不是默认 15%）
  - **FBA Fee**：按尺寸/重量真实计算（4oz $3.06 等）
  - **汇率**：实时 API 拉取（open.er-api.com）

### 5. 项目目录改名（Python import 兼容）
- `poc/01-选品/` → `poc/p01_selection/`（保留 01-选品 软链路供文档引用）

### 6. 评论抓取健壮性
- `_fetch_dp_with_review_loaded` 加了**完整性校验** (< 600KB 视为反爬中间页) + **3 次重试**
- 修复 2026 版 Amazon DOM hook 名变化（`reviewTitle` / `reviewText`）
- 加了 `source_url` 让报告可引用

### 7. ASIN 池 + 候选品强校验
- `modules/asin_pool.py` 全局池
- 所有抓取工具自动入池
- LLM 必须 `validate_candidate(asin)` 才能用
- system prompt 写明禁止虚构

### 8. backend steering 架构（已搭框架）
- `backend/events.py`：Redis pub/sub + chunk seq + catch-up + 取消订阅服务
- `backend/storage.py`：Thread / Message JSONB 持久化
- `backend/stream_job.py`：流式 LLM 工具循环（streamText + UIMessageChunk）
- `backend/app.py`：FastAPI + SSE 端点
- Redis 7.4 已下载并运行（127.0.0.1:6379）

### 9. 证据 + 截图能力
- `modules/evidence.py`：`screenshot_url` / `capture_evidence_for_asin`
- `modules/report_builder.py`：生成含证据来源 + 截图 + 可追溯工具调用链的报告

---

## ⚠️ 部分修复，仍有限制

### "Customers say" 关键词云 + AI 摘要抓取
- 已扩展选择器到 6 个候选位置
- 真实情况：Amazon 详情页有时返回精简版（186KB），有时完整版（2.2MB）
- 已加 600KB 完整性阈值 + 3 次重试
- 仍需进一步：检测精简版后切换 fallback 引擎（pydoll / SeleniumBase）

### 1688 真实采购成本
- 1688 已部署阿里 NC 验证码，公开抓取被挡
- 当前：返回 error + 4 条替代数据源建议（包括"用具体型号反查"/"接 1688 OpenAPI"/"打码服务"/"人工录入"）
- LLM 收到 error 会调用 `search_1688(具体 ASIN 标题翻译)`，命中率会提高

---

## ⏳ 进行中

### 真正用 backend 跑 Agent
- FastAPI + SSE 端点已就绪
- `stream_job.py` 已实现流式 + 工具调用 + 计费
- 需要：
  - 修复 `poc/p01_selection/backend/app.py` 模块路径（已重命名后）
  - 端到端测试 SSE 流（POST /chat → GET /events 接收实时 chunk）

---

## ❌ 还没做

### 前端 SSE 客户端
- 当前只有 Python httpx 测试
- 需要按 frontend steering 做 React + readUIMessageStream + 思考步骤可视化

### Postgres + 多会话/多租户
- 当前 SQLite，单会话池
- 生产改 Postgres，POOL 改成按 thread_id family 分

### 队列（dramatiq actor）
- 当前 `asyncio.create_task` 模拟 worker
- 生产换 dramatiq + Redis broker

---

## 用户输入 → 产出（最终形态）

### 输入
1. 自然语言一句（如"做蓝牙耳机选品调研"）
2. Agent 反问的 6 项澄清答复

### 产出（应该交付的，含证据）
1. **决策报告 markdown**（`reports/final_<ts>.md`）：
   - 候选品决策表（ASIN + 售价 + 评分 + 销量区间 + 决策）
   - 每行带 **截图链接 + 真实页面 URL**（证据可追溯）
2. **截图证据**（`reports/evidence/<asin>_dp.png`）：
   - 每个候选品的 Amazon 详情页截图
   - 关键 BSR 榜单截图
3. **数据溯源** metadata：
   - 关税：HS 8518.30 → 4.5% （USITC HTS）
   - 佣金：8% （Amazon 2026 referral）
   - FBA：$3.06 （Amazon 2026 standard size）
   - 汇率：1USD = 6.787 CNY （open.er-api.com 实时）
4. **完整工具调用链**（每步带时间戳 + 参数 + 结果预览）

---

## 数据真实性矩阵（最新）

| 数据点 | 来源 | 真实性 |
|---|---|---|
| Google Trends | pytrends 实时 | ✅ 真 |
| Amazon BSR Top 30+ | botasaurus + 美国代理 | ✅ 真 |
| 评分/评论数 | 详情页抓取 | ✅ 真 |
| 8-13 条评论文本 | 详情页底部 | ✅ 真 |
| Customers say 关键词 | 详情页 cr-product-insights | 🟡 选择器 6 候选，命中率 ~50% |
| 关税率 | USITC HTS | ✅ 真（2026） |
| Amazon 佣金 | Amazon 公开费率表 | ✅ 真（2026） |
| FBA 配送费 | 按尺寸真实计算 | ✅ 真（standard size 表） |
| 实时汇率 | open.er-api.com | ✅ 真 |
| 1688 采购成本 | 直接抓 | ❌ 被验证码挡，明确 error，让 LLM 走替代路径 |
| BSR→月销量 | 行业经验区间 | 🟡 区间估算，明确 `real_data: false` |
| 候选品 | 必须从 ASIN 池选 | ✅ 真（强制 validate） |
