# 第一环节·选品/市场调研 — 依赖入口（已按用途拆分到 requirements/ 目录）
#
#   requirements/core.txt      核心运行时：LLM + 分析 + 报告导出（必装）
#   requirements/backend.txt   后端服务栈：FastAPI + 队列 + 存储（含 core）
#   requirements/scrapers.txt  可选爬虫引擎：9 级降级链（不装也能跑后端/纯函数）
#
# 默认入口 = backend（含 core），即新克隆能直接把 Agent 后端跑起来：
#   pip install -r requirements.txt
# 需要真实抓取平台数据时再额外：
#   pip install -r requirements/scrapers.txt
-r requirements/backend.txt
